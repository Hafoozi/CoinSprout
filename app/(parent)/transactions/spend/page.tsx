import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Record Spending — CoinSprout',
}

// TODO: Render RecordSpendForm component
export default function RecordSpendPage() {
  return (
    <div className="p-6">
      <p className="text-sm text-gray-400">Record spend — coming soon</p>
    </div>
  )
}
