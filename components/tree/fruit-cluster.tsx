'use client'

import type { FruitCluster as FruitClusterType } from '@/types/domain'

// TODO: Implement fruit cluster overlay
// Renders N fruit SVG icons (N = cluster.count) positioned on the tree branches
// Each icon fades in with a CSS animation when added
// Uses FRUIT_ICON_PATH from constants/fruit-values.ts
export default function FruitClusterComponent({ cluster }: { cluster: FruitClusterType }) {
  return null
}
