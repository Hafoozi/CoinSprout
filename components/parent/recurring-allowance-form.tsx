'use client'

// TODO: Implement recurring allowance configuration form
// Shows ALL children at once, each with their own amount field (default = configured amount)
// Fields per child: name (read-only), amount (editable, default from config), enabled toggle
// Also sets day_of_week (default Sunday) at the family level
// On submit: saves recurring_allowances records for each child
export default function RecurringAllowanceForm() {
  return null
}
